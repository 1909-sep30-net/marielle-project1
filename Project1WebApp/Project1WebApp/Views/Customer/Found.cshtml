@model IEnumerable<Project1.BusinessLogic.Customer>

@{
    ViewData["Title"] = "Found";
}

<h1>Customers Found</h1>
<p>@Model.Count() Customers Found</p>
@if (Model.Count() > 0)
{
    <table class="table">
        <thead>
            <tr>
                <th scope="col">
                    First Name
                </th>
                <th scope="col">
                    Last Name
                </th>

                <th scope="col">
                    @Html.DisplayNameFor(model => model.City)
                </th>

                <th scope="col"></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.FirstName)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.LastName)
                    </td>

                    <td>
                        @Html.DisplayFor(modelItem => item.City)
                    </td>

                    <td>
                        @Html.ActionLink("Get Order History", "GetGetOrderHistory", new { id = item.CustID }) |
                        @Html.ActionLink("Place Order", "SetLocation", new { id = item.CustID })
                    </td>
                </tr>
            }
        </tbody>
    </table>

}
